A collection of various tools to support the Illumina pipeline.

AttachmentMailer.java : Tool to send multiples files in a single email. It
requires a config file EmailHost.config that contains the SMTP hostname of the
sending system. This should be suitably edited based on the need.
